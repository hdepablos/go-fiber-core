# Configuración del Backend para STAGING (AWS Real)
bucket         = "tu-bucket-staging-terraform-state"
key            = "gofibercore/terraform.tfstate"
region         = "us-east-1"
dynamodb_table = "terraform-lock"
encrypt        = true
# No se usan endpoints locales

meta {
  name: paginated-v8
  type: http
  seq: 14
}

post {
  url: {{urlBase}}api/v1/banks/paginated
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
      "sortBy": ["id"],
      "sortDesc": [true],
      "filterBy": ["banks.name:fuzzy"],
      "filterValues": ["Galicia"],
      "rowsPerPage": 15,
      "page": 1
  }
}

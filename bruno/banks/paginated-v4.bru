meta {
  name: paginated-v4
  type: http
  seq: 10
}

post {
  url: {{urlBase}}api/v1/banks/paginated
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
      "sortBy": ["id"],
      "sortDesc": [true],
      "filterBy": ["enabled"],
      "filterValues": [false],
      "rowsPerPage": 15,
      "page": 1
  }
}

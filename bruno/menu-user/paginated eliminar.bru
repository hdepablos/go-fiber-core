meta {
  name: paginated eliminar
  type: http
  seq: 2
}

post {
  url: {{urlBase}}api/v1/menu-users/eliminar/33
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
      "sortBy": ["menu_user.menus.item_name"],
      "sortDesc": [true],
      "filterBy": [],
      "filterValues": [],
      "rowsPerPage": 15
  }
  
}

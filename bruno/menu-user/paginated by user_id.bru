meta {
  name: paginated by user_id
  type: http
  seq: 2
}

post {
  url: {{urlBase}}api/v1/menu-users/by-user/32
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
      "sortBy": ["menu_user.menus.item_name"],
      "sortDesc": [true],
      "filterBy": [],
      "filterValues": [],
      "rowsPerPage": 15
  }
  
}

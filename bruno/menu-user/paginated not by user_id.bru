meta {
  name: paginated not by user_id
  type: http
  seq: 3
}

post {
  url: {{urlBase}}api/v1/menu-users/not-by-user/32
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
      "sortBy": ["menu_user.menus.item_name"],
      "sortDesc": [true],
      "filterBy": [],
      "filterValues": [],
      "rowsPerPage": 15,
      "page": 1
  }
  
}

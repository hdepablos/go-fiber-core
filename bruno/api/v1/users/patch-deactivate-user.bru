meta {
  name: patch-deactivate-user
  type: http
  seq: 10
}

patch {
  url: {{urlBase}}api/v1/users/{{user_id}}/deactivate
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

headers {
  X-Client-Code: bruno
}

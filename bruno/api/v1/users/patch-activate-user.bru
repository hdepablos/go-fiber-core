meta {
  name: patch-activate-user
  type: http
  seq: 9
}

patch {
  url: {{urlBase}}api/v1/users/{{user_id}}/activate
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

headers {
  X-Client-Code: bruno
}

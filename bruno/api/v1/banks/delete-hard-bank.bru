meta {
  name: delete-hard-bank
  type: http
  seq: 6
}

delete {
  url: {{urlBase}}api/v1/banks/hard/{{bank_id}}
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

headers {
  X-Client-Code: bruno
}

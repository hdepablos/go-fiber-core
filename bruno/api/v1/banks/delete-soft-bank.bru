meta {
  name: delete-soft-bank
  type: http
  seq: 5
}

delete {
  url: {{urlBase}}api/v1/banks/soft/{{bank_id}}
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

headers {
  X-Client-Code: bruno
}

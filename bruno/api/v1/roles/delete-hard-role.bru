meta {
  name: delete-hard-role
  type: http
  seq: 6
}

delete {
  url: {{urlBase}}api/v1/roles/hard/{{role_id}}
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

headers {
  X-Client-Code: bruno
}

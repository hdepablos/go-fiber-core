meta {
  name: replicate scenario
  type: http
  seq: 1
}

post {
  url: {{urlBase}}api/v1/process-lifecycle/replicate
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "process_version_id": 3,
    "operator_id": 1
  }
}

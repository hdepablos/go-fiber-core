brew install terraform
brew install pipx terraform
pipx ensurepath

pipx install terraform-local

tflocal --version

tflocal init
brew tap hashicorp/tap
brew install hashicorp/tap/terraform

brew install redis
